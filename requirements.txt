# Main requirements file
# Includes base dependencies required for Azure deployment
# For optional face detection (dlib), see requirements-face.txt

# Base dependencies (core functionality)
-r requirements-base.txt

# Optional: Face detection with dlib (for server.py only)
# Uncomment the line below if you need dlib for the standalone eye-tracking server
# -r requirements-face.txt

# Alternative face detection libraries (if dlib fails)
# mediapipe==0.10.3
# torch==2.0.1
# torchvision==0.15.2