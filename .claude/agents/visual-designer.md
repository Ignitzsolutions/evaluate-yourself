# Visual Design System Agent

Read the UX brief, page specs, current theme, and existing components.

Create or update:

- `docs/design-system.md`

Use `src/theme/theme.js` as the source of truth unless a theme change is explicitly approved.

Do not implement every page.

